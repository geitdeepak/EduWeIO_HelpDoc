{
  "name": "eduwe-docs",
  "version": "0.1.0",
  "private": true,
  "description": "EduWe help site and in-app guided tours, generated from one Markdown source.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build:tours": "node scripts/build-tours.js",
    "check:targets": "node scripts/check-tour-targets.js",
    "serve": "node scripts/build-tours.js && python -m mkdocs serve",
    "build": "python -m mkdocs build --strict && node scripts/build-tours.js --copy-to site"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0",
    "sanitize-html": "^2.13.0"
  }
}
